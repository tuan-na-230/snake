const LEFT = "left";
const RIGHT = "right";
const UP = "up";
const DOWN = "down";
const CONTROL = [LEFT, RIGHT, UP, DOWN];
const DEFAULT_MOVE = LEFT;
const DEFAULT_SPEED = 150;

export { LEFT, RIGHT, UP, DOWN, CONTROL, DEFAULT_MOVE, DEFAULT_SPEED }